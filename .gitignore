# Full size source images. The optimized versions the site actually uses
# live in images/ and are committed. Keeping these out keeps the repository small.
ChatGPT Image*.png
Morris_grey.png

# Local tooling
node_modules/
.DS_Store

# Stray binaries that landed in the folder; never part of the site
*.exe
*.winmd

# Claude session outputs
Claude outputs/
